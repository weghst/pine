package com.weghst.pine.service;

/**
 * 邮件常量定义.
 *
 * @author Kevin Zou (kevinz@weghst.com)
 */
public interface MailConstants {

    /**
     * 发送邮件主机(属性名).
     */
    String MAIL_SENDER_HOST_PROP = "pine.mail.sender.host";

    /**
     * 发送邮件主机端口(属性名).
     */
    String MAIL_SENDER_PORT_PROP = "pine.mail.sender.port";

    /**
     * 发送邮件用户名(属性名).
     */
    String MAIL_SENDER_USERNAME_PROP = "pine.mail.sender.username";

    /**
     * 发送邮件用户密码(属性名).
     */
    String MAIL_SENDER_PASSWORD_PROP = "pine.mail.sender.password";
}
